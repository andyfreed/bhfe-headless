/** @type {import('@faustwp/core').FaustConfig} */
module.exports = {
  // Faust.js configuration
  // Templates and providers will be set up when migrating to full Faust.js Pages Router
  // For App Router, we use direct Apollo Client queries
};

